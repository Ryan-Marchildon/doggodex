import React from "react";

import classes from "./PhotoFrame.module.css";

const photoFrame = (props) => {
  return <div className={classes.PhotoFrame}>[photoFrame]</div>;
};

export default photoFrame;
