import React from "react";

import classes from "./PhotoControls.module.css";

const photoControls = (props) => {
  return <div className={classes.PhotoControls}>[photoControls]</div>;
};

export default photoControls;
